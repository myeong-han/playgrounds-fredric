##Report: Generated by /home/mhkim/playgrounds-fredric/iac/1.manifests/_generate-kube-manifests/3.cert.sh
##Report: 세번째 파라미터를 입력할 경우 해당 도메인에 대한 인증서 발급, 입력하지 않을 경우 env에 등록된 모든 도메인에 대한 인증서 발급.
#############################################
###exec> bash 3.cert.sh harbor.fredric.playground.cld-tls cicd harbor.fredric.playground.cld | kubectl apply -f -
#############################################
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor.fredric.playground.cld-tls
  namespace: cicd
spec:
  secretName: harbor.fredric.playground.cld-tls #생성될 secret Name
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  dnsNames:
  - harbor.fredric.playground.cld
---
